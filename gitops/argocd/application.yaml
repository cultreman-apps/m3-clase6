apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: formatec-production
  namespace: argocd
spec:
  project: default
  source:
    repoURL: repoURL: https://github.com/cultreman-apps/m3-clase6.git
    targetRevision: gitops
    path: gitops/production
  destination:
    server: https://kubernetes.default.svc
    namespace: production
  syncPolicy:
    automated:
      enabled: true
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 1m

